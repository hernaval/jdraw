import React from 'react'

const CompetitionDetailPage = ({ params }: { params: { name: string } }) => {
  return <div>CompetitionDetailPage {params.name}</div>
}

export default CompetitionDetailPage
